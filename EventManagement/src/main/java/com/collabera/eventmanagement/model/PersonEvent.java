package com.collabera.eventmanagement.model;

import javax.persistence.Entity;
import javax.persistence.ForeignKey;
import javax.persistence.JoinColumn;
import javax.persistence.OneToMany;

//@Entity
public class PersonEvent {
	
	/*
	 * @OneToMany
	 * 
	 * @JoinColumn(name = "eventID", nullable = false, foreignKey = @ForeignKey(name
	 * = "events_id")) private long eventID;
	 * 
	 * @OneToMany
	 * 
	 * @JoinColumn(name = "person_ID", nullable = false, foreignKey
	 * = @ForeignKey(name = "person_personId")) private long person_ID;
	 */
}
